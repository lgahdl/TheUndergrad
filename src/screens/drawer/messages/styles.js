import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  mainText: {
    fontSize: 18,
    textAlign: "center",
    marginBottom: 20
  }
});

export default styles;
